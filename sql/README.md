# sql/

SQL schema and queries for the project database (SQLite).

Suggested files to add here as you build Step 2:
- `schema.sql` — CREATE TABLE statements (companies, financial_statements, etc.)
- `load_data.py` — script that loads CSVs from `data/raw/` into the SQLite database
- `queries.sql` — the analytical queries used to pull data into the Jupyter notebooks
